// Reply fetching (the vote source). Placeholder until the research on the
// best non-official-API method lands; returns [] so the rest of the pipeline works.
export async function fetchReplies(_tweetId) {
  return [];
}
